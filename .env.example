# Database
POSTGRES_DB=securevault
POSTGRES_USER=securevault_user
POSTGRES_PASSWORD=securevault_pass

# JWT
JWT_SECRET=super-secret-jwt-key-change-in-production-32ch
JWT_EXPIRY_MINUTES=15
JWT_REFRESH_EXPIRY_DAYS=7

# Encryption (for vault passwords at rest)
ENCRYPTION_KEY=default-encryption-key-change-in-production

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:5000
